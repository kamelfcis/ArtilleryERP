/** Reservation status keys mirrored from frontend constants. */
export const RESERVATION_STATUSES: Record<string, string> = {
  pending: 'pending',
  confirmed: 'confirmed',
  checked_in: 'checked_in',
  checked_out: 'checked_out',
  cancelled: 'cancelled',
  no_show: 'no_show',
}
